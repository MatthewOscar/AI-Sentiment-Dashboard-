# Base URL of the FastAPI backend (no trailing slash, no /api path).
# Leave unset for local development; it defaults to http://127.0.0.1:8000.
# In production set this to your Hugging Face Space URL, for example:
# VITE_API_URL=https://your-user-your-space.hf.space
VITE_API_URL=
